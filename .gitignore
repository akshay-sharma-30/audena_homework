node_modules/
.next/
out/
build/
*.log
.DS_Store

# Local DB (SQLite — regenerated by `npm run setup`)
prisma/dev.db
prisma/dev.db-journal

# Env — only the .example template is committed
.env
.env.local
.env.*.local

# TypeScript build cache (next-env.d.ts itself stays committed — matches `create-next-app` defaults)
*.tsbuildinfo

# Test coverage
/coverage

# Editor
.vscode/
.idea/

# Original brief PDF — not ours to redistribute
Audena Homework - Full-Stack Developer.pdf

# Local AI-assistant tooling — kept on the dev machine, not in the submission
.gitnexus/
.claude/
AGENTS.md
CLAUDE.md
